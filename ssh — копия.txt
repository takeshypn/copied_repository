ѕрежде всего дл€ настройки и запуска OpenSSH сервера необходимо установить пакет openssh-server:
sudo apt-get install openssh-server

Ќо можно также установить OpenSSH командой:
sudo apt-get install ssh

„тобы узнать статус сервиса воспользуйтесь следующей командой:
sudo service ssh status


»так, запустим наш SSH-сервер, так как у мен€ стара€ Debian Linux система на виртуалке, то будем использовать второй способ запуска:
/etc/init.d/ssh start

ѕроверим статус ssh сервиса:
/etc/init.d/ssh status

¬се крутитс€. “еперь узнаем IP адрес нашей машины и перейдем к SSH-клиенту:
ifconfig
127.0.0.1

Ќапоследок узнаем версию ssh клиента:
sudo ssh -V

ќбычно в Ubuntu уже встроен SSH-клиент, и вам не нужно ничего устанавливать, но если все-таки его нет, то установите пакет openssh-client:
sudo apt-get install openssh-client


